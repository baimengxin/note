// string类型是字符串类型，可以使用单引号或者双引号表示
let message: string = 'hello'


const name = 'why';
const age = 18;
const height = 1.70

// 同时也支持 模板字符串 来拼接 字符串 和 变量
const info = `my name is ${name}, age is ${age}, height is ${height}`
console.log(info);  // my name is why, age is 18, height is 1.7


export {}